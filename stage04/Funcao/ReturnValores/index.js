// function anonima
const sum = function(numOne, numTwo) { // parametros da function
    return numOne + numTwo; // quando a função nao tiver a palavra return ela vai retorna undefined entao sempre que eu quiser retorna alguma coisa da minha função uso a a plavra reservada return
};

console.log(sum(2, 3)); // argumentos 