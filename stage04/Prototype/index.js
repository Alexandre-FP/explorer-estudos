/* 
    Prototype (prototipo, aquilo que foi criado antes)

    *prototype-based leanguage
    *prototype chain (cadeias de prototipos)
    *__proto__
*/

console.log(11.0.hasOwnProperty()); // todos os methods que tem um tipo number isso que significa o __proto__ isso para todos os tipo de dados

const pessoa = {
    nome: "Alexandre", 
} 